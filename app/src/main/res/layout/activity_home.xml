<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/zeromeal_background">

    <androidx.core.widget.NestedScrollView
        android:id="@+id/scroll_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="16dp">

            <!-- Header -->
            <include layout="@layout/layout_home_header" />

            <!-- Search Bar -->
            <include layout="@layout/layout_search_bar" />

            <!-- Quick Actions -->
            <include layout="@layout/layout_quick_actions" />

            <!-- Expiring Soon -->
            <include layout="@layout/layout_expiring_soon_section" />

            <!-- Recommended Recipes -->
            <include layout="@layout/layout_recipes_section" />

            <!-- Shopping List -->
            <include layout="@layout/layout_shopping_list_section" />
            
            <View
                android:layout_width="match_parent"
                android:layout_height="40dp"/> <!-- Extra padding at bottom -->

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <include
        android:id="@+id/bottom_navigation"
        layout="@layout/layout_bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
